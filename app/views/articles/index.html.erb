<h1>Articles</h1>

<ol class="list" > 
    <% @articles.each do |article| %>
        <li>
            <h4><%= link_to article.title , article %> </h4>
        </li>
    <%end%>
</ol>

<div class='btn bottom_btn' > <%= link_to "Create Article" , new_article_path%> </div>